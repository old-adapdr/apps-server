---
kind: pipeline
type: exec
name: Deployment-Pipeline

platform:
  os: linux
  arch: amd64
  user: ap

steps:
- name: Deploy
  environment:
    ROOT_PATH:
      from_secret: root_path
  commands:
  - ./home/ap/hs/deploy.py
  when:
    branch:
    - update/pipeline-perm-bug-fix
    event:
    - push
