---
kind: pipeline
type: exec
name: Deployment-Pipeline

platform:
  os: linux
  arch: amd64

steps:
- name: Deploy
  environment:
    ROOT_PATH:
      from_secret: root_path
  commands:
  - ${ROOT_PATH}/deploy.py
  when:
    branch:
    - update/pipeline-perm-bug-fix
    event:
    - push
